# Plan: NovaPanel v5 — Final Completion (Simplified)

## Status: IN PROGRESS

## Goal
Complete NovaPanel v5. Make sure everything works. Stop overcomplicating.

## What's Already Done ✅
- Route prefix collision fix deployed and verified
- Full system test (57/65 endpoints)
- Playwright E2E: 12/12 passing
- Build: 3/3 packages pass
- Server deployed at `192.168.0.211:8732`
- 53 API unit tests passing across 12 test files

## Remaining Work

### Phase A: Expand 7 Test Stubs (1-2 hours)
**Current**: 7 service test files are stubs (`expect(true).toBe(true)`)
**Target**: Real tests using the proven `plugins.service.test.ts` pattern

Files to expand:
1. `billing.service.test.ts` — Plans CRUD, usage records
2. `containers.service.test.ts` — Container CRUD, start/stop
3. `monitoring.service.test.ts` — Metrics, alert rules
4. `security.service.test.ts` — WAF rules, IP allowlists
5. `ssl.service.test.ts` — Certificate CRUD
6. `storage.service.test.ts` — Buckets, access keys
7. `webhooks.service.test.ts` — Webhook CRUD, deliveries

**Pattern to follow** (from `plugins.service.test.ts`):
- Mock `db` with `mockReturnValue` chains (select→from→where→limit)
- Mock external services (logger, nanoid)
- Test happy path + one error case per method
- Keep it simple — if the mock chain gets weird, use `vi.mocked(db.select).mockReturnValueOnce()` to override per test

### Phase B: Build + Verify (15 min)
- Run `pnpm build` — must pass 3/3
- Run `pnpm --filter api test` — must pass all
- Run Playwright E2E — must pass 12/12

### Phase C: Deploy (30 min)
- Build API and web
- Copy dist to server
- Restart novapanel service
- Verify health endpoint

## Acceptance Criteria
- [ ] All 7 test stubs have real passing tests
- [ ] Total test count > 70 (currently 53)
- [ ] pnpm build passes 3/3
- [ ] Playwright E2E passes 12/12
- [ ] Deployed to server and verified

## Constraints
- Do NOT add new features
- Do NOT modify NOT_IMPLEMENTED stubs
- Do NOT change CSS/design
- Keep changes minimal

## Notes
- The `plugins.service.test.ts` pattern works. Just copy it.
- If a mock chain is tricky, simplify the test or skip the edge case.
- Goal is "works correctly", not "perfect test coverage".
