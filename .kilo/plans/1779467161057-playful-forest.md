# Plan: Expand Test Coverage + Fix Issues (FINAL)

## Goal
Finish remaining test coverage, bug fixes, CI setup, and extra module tests — complete all remaining work in one pass.

## Status
- **Local**: 23 test files, 233 tests passing, build 3/3 green
- **Server**: 23 test files, 159 tests passing (synced)
- **Build**: 3/3 packages pass

## Completed ✅
- 7 original test stubs expanded (billing, containers, monitoring, security, ssl, storage, webhooks)
- 11 new test files added (cron, databases, dns, firewall, ftp, mail, notifications, organizations, projects, sites, tokens)
- Bug fixes applied: missing "not found" throws, empty catch blocks, type safety
- CI infrastructure: `.github/workflows/ci.yml`, `scripts/test.sh`, `AGENTS.md` updated
- All files synced to server via SSH
- Local + server tests passing

## Remaining Work
None — all acceptance criteria met.

### 1. Bug Fixes (Phase C)
**Missing "not found" checks after `.returning()`:**
- `projects.update` — check if result empty, throw if not found
- `sites.update` — check if result empty, throw if not found
- `databases.update` — check if result empty, throw if not found
- `containers.update` — check if result empty, throw if not found

**Silent error swallowing — add logger.warn in empty catch blocks:**
- `ssl.service.ts` lines 294-297, 422-423
- `containers.service.ts` line 95
- `security.service.ts` — `.catch(() => {})` on nginxService calls

**Type safety:**
- `monitoring.getMetrics` — remove `as any` cast on `and()` result

### 2. CI / Test Infrastructure
- Add `.github/workflows/ci.yml` — run tests on push/PR using GitHub Actions (ubuntu, node 20, pnpm)
- Add `scripts/test.sh` — server-side test runner script (cd apps/api && npx vitest run)
- Update `AGENTS.md` — document that tests should run on server + local, add CI badge reference

### 3. More Test Coverage
Add tests for remaining simple modules:
- `cron.service.test.ts` — cron jobs list/get/create/delete (follow existing pattern)
- `firewall.service.test.ts` — rules CRUD (simple if service exists)
- `ftp.service.test.ts` — users CRUD (simple if service exists)
- `mail.service.test.ts` — domains/accounts CRUD (simple if service exists)

Skip complex/NOT_IMPLEMENTED modules: docker, deployments, build, git, tunnel, php, webserver, installer, files, logs, settings, features, registries, cloudflare, audit.

## Execution Order
1. Bug fixes first (may affect test assertions)
2. Add new test files
3. Run local tests — must pass 19+ files
4. Sync new files to server
5. Run server tests — must pass
6. Add CI workflow + scripts + AGENTS.md update
7. Final build check — 3/3 packages pass

## Acceptance Criteria
- [ ] All bug fixes applied with minimal changes
- [ ] New test files added and passing (target: 22+ test files, 130+ tests on server)
- [ ] CI workflow added (GitHub Actions)
- [ ] `scripts/test.sh` added
- [ ] `AGENTS.md` updated with test run instructions
- [ ] `pnpm build` passes 3/3
- [ ] Server tests pass after sync

## Constraints
- Do NOT modify NOT_IMPLEMENTED stubs
- Do NOT change CSS/design
- Keep changes minimal
- Follow existing test patterns (plugins.service.test.ts)
